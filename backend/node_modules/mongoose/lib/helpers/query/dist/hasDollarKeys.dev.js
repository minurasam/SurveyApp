'use strict';
/*!
 * ignore
 */

module.exports = function (obj) {
  if (obj == null) {
    return false;
  }

  var keys = Object.keys(obj);
  var len = keys.length;

  for (var i = 0; i < len; ++i) {
    if (keys[i].startsWith('$')) {
      return true;
    }
  }

  return false;
};