"use strict";

var crash = function crash(reason) {
  process.on(reason, function (err) {
    console.error(reason, err.stack);
    process.exit(-1);
  });
};

crash('unhandledRejection');
crash('uncaughtError');
crash('warning');