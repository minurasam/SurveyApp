'use strict';

function _typeof(obj) { if (typeof Symbol === "function" && typeof Symbol.iterator === "symbol") { _typeof = function _typeof(obj) { return typeof obj; }; } else { _typeof = function _typeof(obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }; } return _typeof(obj); }

module.exports = function stringifyFunctionOperators(pipeline) {
  if (!Array.isArray(pipeline)) {
    return;
  }

  var _iteratorNormalCompletion = true;
  var _didIteratorError = false;
  var _iteratorError = undefined;

  try {
    for (var _iterator = pipeline[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
      var stage = _step.value;

      if (stage == null) {
        continue;
      }

      var canHaveAccumulator = stage.$group || stage.$bucket || stage.$bucketAuto;

      if (canHaveAccumulator != null) {
        for (var _i = 0, _Object$keys = Object.keys(canHaveAccumulator); _i < _Object$keys.length; _i++) {
          var key = _Object$keys[_i];
          handleAccumulator(canHaveAccumulator[key]);
        }
      }

      var stageType = Object.keys(stage)[0];

      if (stageType && _typeof(stage[stageType]) === 'object') {
        var stageOptions = stage[stageType];

        for (var _i2 = 0, _Object$keys2 = Object.keys(stageOptions); _i2 < _Object$keys2.length; _i2++) {
          var _key = _Object$keys2[_i2];

          if (stageOptions[_key] != null && stageOptions[_key].$function != null && typeof stageOptions[_key].$function.body === 'function') {
            stageOptions[_key].$function.body = stageOptions[_key].$function.body.toString();
          }
        }
      }

      if (stage.$facet != null) {
        for (var _i3 = 0, _Object$keys3 = Object.keys(stage.$facet); _i3 < _Object$keys3.length; _i3++) {
          var _key2 = _Object$keys3[_i3];
          stringifyFunctionOperators(stage.$facet[_key2]);
        }
      }
    }
  } catch (err) {
    _didIteratorError = true;
    _iteratorError = err;
  } finally {
    try {
      if (!_iteratorNormalCompletion && _iterator["return"] != null) {
        _iterator["return"]();
      }
    } finally {
      if (_didIteratorError) {
        throw _iteratorError;
      }
    }
  }
};

function handleAccumulator(operator) {
  if (operator == null || operator.$accumulator == null) {
    return;
  }

  for (var _i4 = 0, _arr = ['init', 'accumulate', 'merge', 'finalize']; _i4 < _arr.length; _i4++) {
    var key = _arr[_i4];

    if (typeof operator.$accumulator[key] === 'function') {
      operator.$accumulator[key] = String(operator.$accumulator[key]);
    }
  }
}